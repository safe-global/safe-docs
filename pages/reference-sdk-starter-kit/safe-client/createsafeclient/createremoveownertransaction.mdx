import { Tabs } from 'nextra/components'

# `createRemoveOwnerTransaction`

Creates a transaction (to, value, data) to remove an owner to the Safe.

## Usage

{/* <!-- vale off --> */}

<Tabs items={['create-remove-owner-transaction.ts', 'setup.ts']}>
  <Tabs.Tab>
    ```typescript
    const transaction = await safeClient.createRemoveOwnerTransaction({
      ownerAddress,
      threshold
    })

    const txResult = await safeClient.send({ transactions: [transaction] })
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
      import { createSafeClient } from '@safe-global/sdk-starter-kit'
          
      const safeClient = await createSafeClient({
        provider,  
        signer,
        safeAddress
      })
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns

`Promise<TransactionBase>`

Returns a `TransactionBase` transaction that can be used/included in a batch with the `send` method

## Parameters

### `ownerAddress`

- **Type:** `string`

The ethereum address to remove as an owner.

### `threshold` (optional)

- **Type:** `string`

The new threshold for the Safe account.

