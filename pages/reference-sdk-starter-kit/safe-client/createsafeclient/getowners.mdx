import { Tabs } from 'nextra/components'

# `getOwners`

Get the Safe account owners.

## Usage

{/* <!-- vale off --> */}

<Tabs items={['getOwners.ts', 'setup.ts']}>
  <Tabs.Tab>
    ```typescript
    const owners = await safeClient.getOwners()
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    const safeClient = await createSafeClient({
      provider,  
      signer,
      safeAddress
    })
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns

`Promise<string[]>`

Return the list of Safe account owners.