import { Tabs, Callout } from 'nextra/components'
import SafeWebAuthnSharedSignerConfigured from '../events/SafeWebAuthnSharedSignerConfigured.mdx'
import LegacyCallout from '../../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../../components/callouts/IrreversibilityCallout.mdx'



# `configure` (0x0dd9692f)



Sets the signer configuration for the calling account.

The Safe must call this function with a `DELEGATECALL`, as the signer data is stored in
the Safe account's storage.




Defined in [`SafeWebAuthnSharedSigner.sol`](https://github.com/safe-global/safe-modules/tree/passkey/v0.2.1/modules/passkey/contracts/4337/SafeWebAuthnSharedSigner.sol#L137)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function configure(SafeWebAuthnSharedSigner.Signer memory signer) external onlyDelegateCall
    }

    contract Example {
        function example() ... {
            (ISafe safe).configure(
                Signer
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `signer`

  - **Type:** `struct SafeWebAuthnSharedSigner.Signer`

  The new signer data to set for the calling account.

  ```solidity focus=2
  (ISafe safe).configure(
    Signer
  );
  ```




## Events

<SafeWebAuthnSharedSignerConfigured />

