import { Tabs, Callout } from 'nextra/components'

import LegacyCallout from '../../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../../components/callouts/IrreversibilityCallout.mdx'
import VirtualCallout from '../../../../../components/callouts/VirtualCallout.mdx'
import ModuleCallout from '../../../../../components/callouts/ModuleCallout.mdx'
import PreApprovedCallout from '../../../../../components/callouts/PreApprovedCallout.mdx'




# `executeUserOpWithErrorString` (0x541d63c8)



Executes a user operation provided by the entry point and returns error message on failure.




Defined in [`Safe4337Module.sol`](https://github.com/safe-global/safe-modules/tree/4337/v0.3.0-1/modules/4337/contracts/Safe4337Module.sol#L185).


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function executeUserOpWithErrorString(
            address to,
            uint256 value,
            bytes memory data,
            uint8 operation
        ) external onlySupportedEntryPoint
    }

    contract Example {
        function example() ... {
            (ISafe safe).executeUserOpWithErrorString(
                0x...,
                0,
                "0x...",
                0
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `to`

  - **Type:** `address`

  Destination address of the user operation.

  ```solidity focus=2
  (ISafe safe).executeUserOpWithErrorString(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `value`

  - **Type:** `uint256`

  Ether value of the user operation.

  ```solidity focus=3
  (ISafe safe).executeUserOpWithErrorString(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `data`

  - **Type:** `bytes`

  Data payload of the user operation.

  ```solidity focus=4
  (ISafe safe).executeUserOpWithErrorString(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `operation`

  - **Type:** `uint8`

  Operation type of the user operation.

  ```solidity focus=5
  (ISafe safe).executeUserOpWithErrorString(
    0x...,
    0,
    "0x...",
    0
  );
  ```





