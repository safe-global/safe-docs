import { Tabs, Callout } from 'nextra/components'
import ResetAllowance from '../events/ResetAllowance.mdx'
import LegacyCallout from '../../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../../components/callouts/IrreversibilityCallout.mdx'



# `resetAllowance` (0xc19bf50e)



Allows to reset the allowance for a specific delegate and token.




Defined in [` AllowanceModule.sol`](https://github.com/safe-global/safe-modules/tree/allowance/v0.1.1/modules/allowances/contracts/AllowanceModule.sol#L123)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function resetAllowance(address delegate, address token) public
    }

    contract Example {
        function example() ... {
            (ISafe safe).resetAllowance(
                0x...,
                0x...
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `delegate`

  - **Type:** `address`

  Delegate whose allowance should be updated.

  ```solidity focus=2
  (ISafe safe).resetAllowance(
    0x...,
    0x...
  );
  ```


### `token`

  - **Type:** `address`

  Token contract address.

  ```solidity focus=3
  (ISafe safe).resetAllowance(
    0x...,
    0x...
  );
  ```




## Events

<ResetAllowance />

