import { Tabs } from 'nextra/components'

### `getTransactions`

Returns all the transactions associated with the Safe connected to the [`SafeProvider`](../safeprovider.mdx).

## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.tsx']}>
  <Tabs.Tab>
    ```typescript
    import { useSafe } from '@safe-global/safe-react-hooks'

    function Transactions() {
      const { getTransactions } = useSafe()
      const { data, error, isLoading } = getTransactions()

      if (isLoading) return <>Loading...</>
      
      if (error) return <>Error: {error.message}</>

      return (
        <>
          {JSON.stringify(data, null, 2)}
        </>
      )
    }

    export default Transactions
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns

`UseTransactionsReturnType`

A [query result](https://tanstack.com/query/v5/docs/framework/react/reference/useQuery) object with the list of transactions in the `data` property.
- The `data` property [format](https://github.com/safe-global/safe-core-sdk/blob/9fca59736c418dc6253404b4a7784e2245d83312/packages/api-kit/src/types/safeTransactionServiceTypes.ts#L212-L216)

## Parameters

- **Type**: `UseTransactionsParams`

Parameters to customize the hook behavior.

```typescript
const result = getTransactions(params)
```

### `config` (Optional)

- **Type**: [`SafeConfig`](../safeconfig.mdx)

The configuration used instead of the one from the nearest [`SafeProvider`](../safeprovider.mdx).

<Tabs items={['index.tsx', 'config.ts']}>
  <Tabs.Tab>
    ```typescript
    import config from './index.ts'

    const result = getTransactions({
      config
    })
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    import { createConfig } from '@safe-global/safe-react-hooks'
    import { sepolia } from 'viem/chains'

    const config = createConfig({
      chain: sepolia,
      provider,
      signer,
      safeOptions: {
        owners: ['0x...', '0x...', '0x...'],
        threshold: 2
      }
    })
    ```
  </Tabs.Tab>
</Tabs>
