import { Tabs } from 'nextra/components'

# `isSignerConnected`

Indicates whether the signer is connected. Check this before using any method that requires a signer, such as sending transactions or signing messages.

## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.tsx']}>
  <Tabs.Tab>
    ```typescript
    import { useSafe } from '@safe-global/safe-core-sdk-react-hooks'

    const IsSignerConnected = () => {
      const { isSignerConnected } = useSafe()

      return (
        <>
          <h3>{isSignerConnected ? 'Signer is connected' : 'No signer connected'}</h3>
        </>
      )
    }

    export default IsSignerConnected
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns

`boolean`
