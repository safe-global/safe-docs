import { Tabs, Callout } from 'nextra/components'

import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'



# `encodeTransactionData` (0xe86637db)



Returns the pre-image of the transaction hash (see getTransactionHash).




Defined in [` Safe.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.4.1/contracts/Safe.sol#L383)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function encodeTransactionData(address to,uint256 value,bytes calldata data,Enum.Operation operation,uint256 safeTxGas,uint256 baseGas,uint256 gasPrice,address gasToken,address refundReceiver,uint256 _nonce) public view returns (bytes memory)
    }

    contract Example {
        function example() ... {
            (ISafe safe).encodeTransactionData(
                0x...,
                0,
                0,
                0
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns



  - **Type:** `bytes`

  Transaction hash bytes.




## Parameters

### `to`

  - **Type:** `address`

  Destination address.

  ```solidity focus=2
  (ISafe safe).encodeTransactionData(
    0x...,
    0,
    0,
    0
  );
  ```


### `operation`

  - **Type:** `enum Enum.Operation`

  Operation type.

  ```solidity focus=3
  (ISafe safe).encodeTransactionData(
    0x...,
    0,
    0,
    0
  );
  ```


### `gasPrice`

  - **Type:** `uint256`

  Maximum gas price that should be used for this transaction.

  ```solidity focus=4
  (ISafe safe).encodeTransactionData(
    0x...,
    0,
    0,
    0
  );
  ```


### `_nonce`

  - **Type:** `uint256`

  Transaction nonce.

  ```solidity focus=5
  (ISafe safe).encodeTransactionData(
    0x...,
    0,
    0,
    0
  );
  ```





