import { Tabs, Callout } from 'nextra/components'

import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'



# `tokensReceived` (0x0023de29)



Called by the ERC777 token contract after a successful transfer or a minting operation.




Defined in [` ERC777TokensRecipient.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.4.1/contracts/interfaces/ERC777TokensRecipient.sol#L19)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function tokensReceived(address operator,address from,address to,uint256 amount,bytes calldata data,bytes calldata operatorData) external
    }

    contract Example {
        function example() ... {
            (ISafe safe).tokensReceived(
                0x...,
                0
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `operator`

  - **Type:** `address`

  The address of the operator performing the transfer or minting operation.

  ```solidity focus=2
  (ISafe safe).tokensReceived(
    0x...,
    0
  );
  ```


### `amount`

  - **Type:** `uint256`

  The amount of tokens that were transferred or minted.

  ```solidity focus=3
  (ISafe safe).tokensReceived(
    0x...,
    0
  );
  ```





