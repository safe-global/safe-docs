import { Tabs, Callout } from 'nextra/components'
import SignMsg from '../events/SignMsg.mdx'
import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'
import VirtualCallout from '../../../../components/callouts/VirtualCallout.mdx'
import ModuleCallout from '../../../../components/callouts/ModuleCallout.mdx'
import PreApprovedCallout from '../../../../components/callouts/PreApprovedCallout.mdx'




# `signMessage` (0x85a5affe)



Marks a message (`_data`) as signed.

Can be verified using EIP-1271 validation method by passing the pre-image of the message hash and empty bytes as the signature.




Defined in [`SignMessageLib.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.4.1/contracts/libraries/SignMessageLib.sol#L22).


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function signMessage(bytes calldata _data) external
    }

    contract Example {
        function example() ... {
            ISafe(0x...).signMessage(
                "0x..."
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `_data`

  - **Type:** `bytes`

  Arbitrary length data that should be marked as signed on the behalf of address(this).

  ```solidity focus=2
  ISafe(0x...).signMessage(
    "0x..."
  );
  ```




## Events

<SignMsg />

