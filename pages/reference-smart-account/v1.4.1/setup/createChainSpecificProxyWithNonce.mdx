import { Tabs, Callout } from 'nextra/components'
import ProxyCreation from '../events/ProxyCreation.mdx'
import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'



# `createChainSpecificProxyWithNonce` (0xec9e80bb)



Deploys a new chain-specific proxy with `_singleton` singleton and `saltNonce` salt. Optionally executes an initializer call to a new proxy.

Allows to create a new proxy contract that should exist only on 1 network (e.g. specific governance or admin accounts)
by including the chain id in the create2 salt. Such proxies cannot be created on other networks by replaying the transaction.




Defined in [` SafeProxyFactory.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.4.1/contracts/proxies/SafeProxyFactory.sol#L67)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function createChainSpecificProxyWithNonce(address _singleton,bytes memory initializer,uint256 saltNonce) public returns (SafeProxy proxy)
    }

    contract Example {
        function example() ... {
            (ISafe safe).createChainSpecificProxyWithNonce(
                0x...
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `_singleton`

  - **Type:** `address`

  Address of singleton contract. Must be deployed at the time of execution.

  ```solidity focus=2
  (ISafe safe).createChainSpecificProxyWithNonce(
    0x...
  );
  ```




## Events

<ProxyCreation />

