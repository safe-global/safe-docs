import { Tabs, Callout } from 'nextra/components'

import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'

<LegacyCallout />

# `requiredTxGas` (0xc4ca3a9c)



Deprecated in favor of common/StorageAccessible.sol and will be removed in next version.
Allows to estimate a Safe transaction.
This method is only meant for estimation purpose, therefore the call will always revert and encode the result in the revert data.
Since the `estimateGas` function includes refunds, call this method to get an estimated of the costs that are deducted from the safe with `execTransaction`




Defined in [`GnosisSafe.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.3.0/contracts/GnosisSafe.sol#L315)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function requiredTxGas(address to,uint256 value,bytes calldata data,Enum.Operation operation) external returns (uint256)
    }

    contract Example {
        function example() ... {
            (ISafe safe).requiredTxGas(
                0x...,
                0,
                "0x...",
                0
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Returns



  - **Type:** `uint256`

  Estimate without refunds and overhead fees (base transaction and payload data gas costs).




## Parameters

### `to`

  - **Type:** `address`

  Destination address of Safe transaction.

  ```solidity focus=2
  (ISafe safe).requiredTxGas(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `value`

  - **Type:** `uint256`

  Ether value of Safe transaction.

  ```solidity focus=3
  (ISafe safe).requiredTxGas(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `data`

  - **Type:** `bytes`

  Data payload of Safe transaction.

  ```solidity focus=4
  (ISafe safe).requiredTxGas(
    0x...,
    0,
    "0x...",
    0
  );
  ```


### `operation`

  - **Type:** `enum Enum.Operation`

  Operation type of Safe transaction.

  ```solidity focus=5
  (ISafe safe).requiredTxGas(
    0x...,
    0,
    "0x...",
    0
  );
  ```





