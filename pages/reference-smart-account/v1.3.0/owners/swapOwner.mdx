import { Tabs, Callout } from 'nextra/components'
import RemovedOwner from '../events/RemovedOwner.mdx'
import AddedOwner from '../events/AddedOwner.mdx'
import LegacyCallout from '../../../../components/callouts/LegacyCallout.mdx'
import OnlySafeTxCallout from '../../../../components/callouts/OnlySafeTxCallout.mdx'
import ReentrancyCallout from '../../../../components/callouts/ReentrancyCallout.mdx'
import IrreversibilityCallout from '../../../../components/callouts/IrreversibilityCallout.mdx'

<LegacyCallout />

# `swapOwner` (0xe318b52b)



Replaces the owner `oldOwner` in the Safe with `newOwner`.

Allows to swap/replace an owner from the Safe with another address.
<OnlySafeTxCallout />




Defined in [` OwnerManager.sol`](https://github.com/safe-global/safe-smart-account/tree/v1.3.0/contracts/base/OwnerManager.sol#L94)


## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.sol']}>
  <Tabs.Tab>
    ```solidity
    interface ISafe {
        function swapOwner(address prevOwner,address oldOwner,address newOwner) public authorized
    }

    contract Example {
        function example() ... {
            (ISafe safe).swapOwner(
                0x...,
                0x...,
                0x...
            );
        }
    }
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}



## Parameters

### `prevOwner`

  - **Type:** `address`

  Owner that pointed to the owner to be replaced in the linked list

  ```solidity focus=2
  (ISafe safe).swapOwner(
    0x...,
    0x...,
    0x...
  );
  ```


### `oldOwner`

  - **Type:** `address`

  Owner address to be replaced.

  ```solidity focus=3
  (ISafe safe).swapOwner(
    0x...,
    0x...,
    0x...
  );
  ```


### `newOwner`

  - **Type:** `address`

  New owner address.

  ```solidity focus=4
  (ISafe safe).swapOwner(
    0x...,
    0x...,
    0x...
  );
  ```




## Events

<RemovedOwner />
<AddedOwner />

