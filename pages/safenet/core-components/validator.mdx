# Validator

The Validator represents a service that verifies that the Processor correctly settles a transaction according to the guarantees signed by the End User. In case of a fraudulent or invalid settlement, the Validator can challenge the settlement, launching a dispute process on which the processor must prove the correctness of the transaction.