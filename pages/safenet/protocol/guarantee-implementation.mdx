# Guarantee Implementation

* Smart Contract
* Registered at one guarantee engine
* Can have different semantics around defaults (such as enabled or disabled by default, or required vs default configuration).