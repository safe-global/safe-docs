import { Tabs } from 'nextra/components'

# `createRejectionTransaction`

Returns a Safe transaction ready to be signed by the owners of the connected Safe that invalidates the pending Safe transactions with a specific nonce.

## Usage

{/* <!-- vale off --> */}

<Tabs items={['example.ts', 'setup.ts']}>
  <Tabs.Tab>
    ```typescript
    import {
      MetaTransactionData,
      OperationType
    } from '@safe-global/types-kit'
    import { protocolKit } from './setup.ts'

    const nonce = 123

    const rejectionTransaction = await protocolKit.createRejectionTransaction(nonce)
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    import Safe from '@safe-global/protocol-kit'

    export const protocolKit = Safe.init({
      provider,
      signer,
      safeAddress: '0x...'
    })
    ```
  </Tabs.Tab>
</Tabs>

{/* <!-- vale on --> */}

## Parameters

### `nonce`

- **Type**: `number`

The nonce of the transaction(s) that will be rejected.

```typescript focus=2
const rejectionTransaction = await protocolKit.createRejectionTransaction(
  123
)
```

## Returns

`Promise<SafeTransaction>`

The Safe transaction that invalidates the pending Safe transaction(s).
